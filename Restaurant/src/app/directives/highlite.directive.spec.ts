import { HighliteDirective } from './highlite.directive';

describe('HighliteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighliteDirective();
    expect(directive).toBeTruthy();
  });
});
